<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.willi.e_slang">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Dictionary"/>
        <activity android:name=".AddWord1" />
        <activity android:name=".AddWord2" />
        <activity android:name=".AddWord3" />
        <activity android:name=".WordScreen" />
        <activity android:name=".TagScreen" />
        <activity android:name=".EditWord"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".VideoScreen" />
    </application>

</manifest>